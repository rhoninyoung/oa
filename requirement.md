这份文件已经按照资深OA系统架构师的视角进行了重构。为了让 **Agent Copilot / Coder** 能够更精准地理解逻辑、生成代码，我对文档进行了以下优化：
 1. **结构化逻辑**：将模糊的文字描述转为“角色-权限-状态机”模型。
 2. **显式约束**：明确定义了自动保存、并发控制、循环依赖检测等硬性逻辑规则。
 3. **触发器模式**：采用“触发条件-执行动作-预期结果”的模式，方便 Agent 编写 Event Handler。
 4. **Markdown 标准化**：图片与对应功能块深度耦合，确保上下文关联。
# OA 平台项目管理需求规格说明书 (Agent-Friendly 版)
| 字段 | 内容 |
|---|---|
| **文档名称** | OA 平台项目管理需求规格说明书 |
| **文档版本** | V1.0.0 (Refined for AI Agent) |
| **架构审核** | Gemini (Senior OA Architect) |
| **编写日期** | 2026-04-22 |
## 0. 修订历史
| 时间 | 变更人 | 类型 | 描述 |
|---|---|---|---|
| 2026.04.10 | 胡孟瑶 | 增加 | 初始版本 V1.0.0 |
## 1. 全局定义与角色权限 (System Overview)
### 1.1 核心业务实体
 * **项目 (Project)**: 一级标题，包含多个迭代。
 * **迭代 (Iteration)**: 二级标题，项目管理的实际落地单位。
 * **排期表 (WBS Table)**: 核心组件，支持 Excel 级交互。
### 1.2 角色权限矩阵
| 权限项 | 组长 (Group Leader) | 项目经理 (Project Manager) |
|---|---|---|
| **本组排期** | 编辑、提交、撤销、重新排期（已审批后） | 查看、审批、重新排期 |
| **跨组排期** | 只读查看 | 查看、审批 |
| **项目总表** | 只读查看 | 增删行、调整时间、分配负责人 |
| **依赖管理** | 配置/编辑本组任务依赖 | 全局配置/编辑所有任务依赖 |
## 2. 界面布局与导航 (Navigation)
### 2.1 模块入口
 * **路径**: 登录系统 -> 侧边栏: 项目管理。
 * **展示形式**: 树形列表，按 项目(L1) -> 迭代(L2) 展示。
## 3. 小组排期功能逻辑 (Group Scheduling)
### 3.1 状态机定义 (State Machine)
排期表必须处于以下状态之一：
 1. **Pending (未提交)**: 初始状态或撤回后的状态，允许编辑。
 2. **Reviewing (审批中)**: 组长提交后，锁定编辑，等待 PM 审批。
 3. **Approved (已审批)**: 审批通过，数据同步至总表。
 4. **Rejected (已退回)**: 审批拒绝，允许组长修改重新提交。
### 3.2 交互触发规则
 * **双击入口**: 双击迭代名称进入详情页，展示各组排期状态。
 * **Excel 级表格能力**:
   * **快捷键支持**: Ctrl+C/V/Z/Y/B，Alt+Enter（换行）。
   * **交互规范**: 拖拽选中、自动换行、单元格格式设置、行列冻结。
   * **UI 参照**: WBS 表设计需完全对齐 Excel 体验。
### 3.3 核心逻辑触发器 (Triggers)
| 动作 | 触发条件 | 执行动作 |
|---|---|---|
| **自动保存** | 编辑中, 每 30s | 异步持久化草稿。 |
| **提交 (Submit)** | 状态 in [Pending, Rejected, Approved] AND 内容非空 | 1. 状态变更为 Reviewing；2. 推送钉钉 OA 助手通知 PM。 |
| **撤回 (Withdraw)** | 状态 == Reviewing | 1. 状态恢复为 Pending；2. 保留数据；3. 移除 PM 待办。 |
## 4. 项目总排期与数据同步 (Master Schedule & Sync)
### 4.1 总表视图
总表自动汇总所有 Approved 状态的小组排期。
### 4.2 双向联动规则 (Data Linkage)
 * **正向同步 (Master -> Group)**:
   1. PM 在总表中 Add Row，指定 负责人 并 Save。
   2. 任务自动追加至对应组长排期表末尾。
   3. 系统触发钉钉通知给负责人及组长。
 * **反向同步 (Group -> Master)**:
   1. 小组排期 Reviewing -> Approved。
   2. 全量同步/更新总表对应数据。
 * **删除约束**: 仅允许 PM 删除在总表中手动新增的行，系统同步的小组行不可删除。
### 4.3 审批流与重新排期
 * **同意 (Agree)**: 同步至总表。
 * **拒绝 (Reject)**: 必填理由（200字），状态转为 Rejected。
 * **重新排期 (Re-schedule)**:
   * **触发**: PM 点击“重新排期”按钮。
   * **逻辑**: 即使已审批，点击后该组数据从总表移除，状态转为 Rejected，通知组长调整。
   * **冲突处理**: 若组长正在编辑，系统弹出覆盖风险提示。
## 5. 任务依赖关系逻辑 (Dependency Management)
### 5.1 约束算法 (Algorithms)
 1. **1-to-1 限制**: 每个任务仅支持设置 **1** 个前置依赖项。
 2. **循环依赖检测 (Cycle Detection)**:
   * 禁止 A -> B -> A。
   * 禁止链式循环 A -> B -> C -> A。
   * **处理**: 弹窗提示并拦截保存。
 3. **时间联动 (Time Propagation)**:
   * Dependency_Task.Finish_Time 变更。
   * Target_Task.Start_Time 自动更新。
   * 计算规则：考虑工作日历，屏蔽法定节假日/周六日。
### 5.2 交互流程
 1. 点击单元格“选择依赖项” -> 其他列置灰 -> 选择目标行。
 2. 高亮显示并自动保存。
## 6. 辅助模块 (Auxiliary Features)
### 6.1 备忘录 (Memo)
 * **功能**: 全屏大白板，支持富文本。
 * **图片支持**: JPG/PNG/GIF, < 20MB。
 * **保存逻辑**: 30s 自动保存；关闭时若有未保存内容需弹窗确认。
### 6.2 导出 (Export)
 * **格式**: .xlsx。
 * **要求**: 必须包含图片备忘录及 PM 新增的所有内容，保证内容完整。
### **Agent 提示词建议 (For Copilot)**
> "请基于上述 Markdown 文档，使用 React + TypeScript 实现 小组排期(WBS Table)。要求：
>  1. 实现状态机（Pending, Reviewing, Approved, Rejected）。
>  2. 表格组件需支持键盘快捷键和 Alt+Enter 换行。
>  3. 集成 30s 自动保存逻辑。
>    4. 实现循环依赖检测算法（深度优先遍历）。"
> 