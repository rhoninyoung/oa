# OA MVP — PDF 需求覆盖 Gap 分析

> 分析日期：2026-04-29
> 对应文档：`OA_requirement.pdf`（某研究所 OA 项目管理系统需求规格说明书）
> 当前版本：Phase 5–11 完成后

---

## 一、总体判断

当前实现完成度约 **45%**。核心工作流（PENDING→REVIEWING→APPROVED/REJECTED）正确实现，WBS 表格、日历/甘特图/看板视图、通知系统、字段级权限均已落地。

但 PDF 是一个完整的 OA 项目管理系统，当前实现更接近一个**排期审批工具**，距离完整 OA 还有大量核心功能缺口。最严重的缺口集中在：无登录认证、无 Dashboard、无进度管理、无站内通知。

---

## 二、章节 2：用户角色与权限

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| 4 种角色：PM / 项目总调度 / GL / 组员 | 只有 GL 和 PM 两种 | **缺失：项目总调度（PM 助手，读写主计划）、组员（仅看本人任务）** |
| 详细权限矩阵（R/W per 角色/功能） | `permit()` 覆盖部分行级权限 | **不完整：系统管理/主计划分解/任务分配等权限未严格校验** |
| 用户密码加密存储（bcrypt） | 明文无加密 | **P0 安全缺口** |
| 部门/组织架构管理 | 只有 Group，无 Dept | **缺失：部门管理** |

---

## 三、章节 3：业务流程

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| 组长制定计划（任务分解到成员） | ✅ 已实现 | — |
| 提交审批 + 生成评审记录 | ⚠️ 状态变更但 ApprovalRecord 未写入 | **缺失：提交/审批时未实际创建 ApprovalRecord 记录** |
| 计划审批流程 | ✅ 已实现 | — |
| **组员每日进度填报** | ❌ 完全未实现 | **P1：任务无 progress_percent，无每日填报入口** |
| **组长汇总统计进度** | ❌ 无进度统计 | **P1：无法按人/按组统计完成率** |
| **项目经理查看全部进度** | ❌ 无进度视图 | **P1：PM 无法感知各组实际完成情况** |
| **进度预警（绿/黄/橙/红四级）** | ❌ 无预警 | **P2** |
| **计划调整申请（重新审批流）** | ⚠️ 再次 submit 可触发 | **不完整：调整历史版本未单独管理** |

---

## 四、章节 4.1：项目管理

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| 项目基本信息（编号/名称/经理/日期/状态） | ⚠️ 只有 name | **缺失：project_code、pm_user_id、start_date、end_date、status** |
| 项目状态（筹备中/进行中/已结项/已挂起/已取消） | ❌ 无 | **P1：项目状态枚举未实现** |
| **项目级里程碑** | ❌ 无 | **P1：里程碑模型 + 甘特图里程碑标记** |
| 主计划制定（PM 制定整体里程碑） | ❌ 无 | **P1：PM 主计划编辑入口缺失** |
| 分组计划编制（GL 细分任务） | ✅ 已实现 | — |

---

## 五、章节 4.2：任务管理

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| 任务基本信息（编号/名称/负责人/日期/工期/依赖/优先级/状态） | ⚠️ 缺编号、优先级、状态 | **缺失：task_code、priority、task_status（未开始/进行中/已完成）** |
| **完成百分比（progress_percent）** | ❌ 无 | **P1：0-100 进度字段 + UI 填报** |
| WBS 多级树形（项目→工作包→子任务→细项） | ⚠️ 只有扁平任务列表 | **P1：真正的嵌套 WBS 树（parent_task_id）** |
| 自动汇总下级进度和工期 | ❌ 无 | **P2** |
| **依赖类型（FS/SS/FF/SF 四种）** | ⚠️ 只有 FS | **P2：当前只实现了完成-开始（FS），SS/FF/SF 未实现** |
| 拖拽调整任务顺序 | ✅ Alt+拖拽已实现 | — |

---

## 六、章节 4.3：进度管理

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| 组员每日填报任务进展 | ❌ 无 | **P1** |
| 按项目/工作包/任务层级查看进度 | ❌ 无 | **P1** |
| 系统自动计算加权完成率 | ❌ 无 | **P1** |
| **进度预警（绿/黄/橙/红）** | ❌ 无 | **P2** |
| 项目整体进度看板 | ❌ 无 | **P2** |
| 组别/个人进度视图 | ❌ 无 | **P2** |

---

## 七、章节 4.4：评审管理

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| 评审流程（提交→待审批→审批中→通过/拒绝） | ⚠️ 缺"审批中"中间态 | **缺失：中间态"审批中"** |
| 评审状态：草稿/待审批/审批中/已通过/已拒绝/已调整 | ⚠️ 只有 PENDING/REVIEWING/APPROVED/REJECTED | **缺失：草稿(DRAFT)、已调整(ADJUSTED)** |
| 评审记录表（pm_review） | ⚠️ ApprovalRecord 模型存在但未写入 | **P1：实际创建评审记录** |
| 审批意见（200字限制） | ✅ 已实现 | — |

---

## 八、章节 4.5：沟通协作

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| **系统消息通知（站内信）** | ❌ 无 | **P1：站内通知面板 + 未读计数** |
| 邮件通知 | ✅ 已实现（Outbox + nodemailer） | — |
| 钉钉 Webhook | ✅ 已实现 | — |
| 任务进度滞后通知（组长 + PM） | ❌ 无 | **P2：进度监控触发通知** |
| 里程碑临近提醒 | ❌ 无 | **P2** |
| 计划调整申请通知 | ❌ 无 | **P2** |

---

## 九、章节 4.6：系统管理

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| 用户增删改查 | ⚠️ 后端 API 有，前端无 UI | **P1：用户管理页面** |
| 角色分配 | ⚠️ 只有 enum，无角色管理 UI | **P1：角色配置页面** |
| 部门管理 | ❌ 无 | **P1：部门 CRUD** |
| 操作日志 | ⚠️ activityLog 存了但无管理 UI | **P2：日志查询页面** |
| 登录日志 | ❌ 无 | **P2** |
| 审批日志 | ⚠️ ApprovalRecord 未写入 | **P1：关联到评审记录** |

---

## 十、章节 5：页面原型

| PDF 需求 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| **登录页面**（用户名 + 密码 + 记住密码） | ❌ 无 | **P0：无登录页，用户直接进入系统** |
| **首页/仪表盘**（待处理事项/项目进度/里程碑提醒/快捷入口） | ❌ 无 | **P0：缺失 Dashboard 页面** |
| 项目列表（筛选/新建/编辑/删除） | ⚠️ 项目树侧边栏有，但不完整 | **P1：独立项目列表页** |
| **WBS 任务列表**（展开折叠/右键菜单/甘特图） | ⚠️ WBS 已实现，甘特图在独立视图 | **P1：WBS 内嵌甘特图区域（参考 PDF 图3布局）** |
| **审批详情页**（左侧预览/右侧操作/下方历史） | ⚠️ approval-panel 嵌入主视图，非独立页 | **P1：独立审批详情页** |
| 进度看板 | ❌ 无 | **P2** |
| 系统管理页面 | ⚠️ 无独立管理后台 | **P1：用户/角色/部门/日志管理页** |

---

## 十一、章节 6：数据字典

| PDF 字段 | Prisma 现状 | Gap 说明 |
|----------|-------------|----------|
| sys_user.password | ❌ 无 | **P0：密码字段 + bcrypt 加密** |
| sys_user.username | ❌ 无（只有 name） | **P1：唯一登录用户名** |
| sys_user.phone | ❌ 无 | **P2** |
| sys_user.role_id → Role enum | ⚠️ 只有 GL/PM | **P1：需扩展角色（项目总调度/组员）** |
| sys_user.dept_id | ❌ 无 | **P1：部门关联** |
| sys_user.status（0/1 禁用启用） | ❌ 无 | **P2** |
| pm_project.project_code | ❌ 无 | **P1：自动生成项目编号** |
| pm_project.pm_user_id | ❌ 无（只有 relation） | **P1：项目经理关联** |
| pm_project.start_date / end_date | ❌ 无 | **P1** |
| pm_project.status | ❌ 无 | **P1：项目状态枚举** |
| pm_project.description | ❌ 无 | **P2** |
| pm_task.progress_percent | ❌ 无 | **P1：进度百分比 0-100** |
| pm_task.priority | ❌ 无 | **P2：高/中/低** |
| pm_task.task_code | ❌ 无 | **P2：自动生成** |
| pm_task.parent_task_id | ❌ 无（扁平列表） | **P1：嵌套 WBS 父子关系** |
| pm_task.dependency_type（FS/SS/FF/SF） | ❌ 无 | **P2：依赖类型枚举** |
| pm_task.status（未开始/进行中/已完成/已延期） | ❌ 无 | **P1：任务自身状态** |
| pm_review（评审记录表） | ⚠️ ApprovalRecord 存在但未用 | **P1：完整评审记录写入** |
| pm_progress（进度记录表） | ❌ 无 | **P1：每日进度历史** |

---

## 十二、章节 7：接口设计

| PDF 接口 | 实现状态 | Gap 说明 |
|----------|----------|----------|
| POST /api/auth/login | ❌ 无 | **P0：JWT 登录接口** |
| GET /api/projects（分页 + 筛选） | ⚠️ 有但无分页 | **P1：分页 + 状态筛选** |
| GET /api/tasks/tree（树形） | ❌ 无（扁平列表） | **P1：嵌套树形返回** |

---

## 十三、章节 8：非功能性需求

| 需求 | 现状 | Gap |
|------|------|-----|
| 密码加密（bcrypt） | ❌ 无 | **P0** |
| 重要操作记录日志 | ⚠️ activityLog 有但不完整 | **P1** |
| 防止 SQL 注入/XSS | ❌ 无 | **P0：输入校验** |
| 敏感数据脱敏 | ❌ 无 | **P2** |
| 50 用户并发支持 | ⚠️ 无压测验证 | **P2** |
| 主流浏览器兼容 | ✅ | — |
| 移动端响应式 | ❌ 无 | **P2** |

---

## 十四、Gap 汇总分级

### P0 — 必须修复（安全/合规/核心流程断裂）

| # | Gap | 影响 |
|---|-----|------|
| 1 | 无登录页 + JWT 鉴权 | 任何人可伪造身份操作数据，安全合规致命伤 |
| 2 | 无 Dashboard/首页 | 用户无法看到项目总览和待办事项，PM/GL 无入口 |
| 3 | 用户密码明文存储 | 严重安全漏洞 |
| 4 | 无 SQL 注入/XSS 防护 | API 端口暴露时可被直接攻击 |
| 5 | 项目基本信息不完整（缺编号/日期/状态） | 项目管理模块不完整 |

### P1 — 核心功能缺失（影响日常使用）

| # | Gap | 影响 |
|---|-----|------|
| 6 | 任务无 progress_percent（无进度填报） | 进度管理流程完全断裂 |
| 7 | 无站内消息通知 | 用户感知不到审批状态变更 |
| 8 | ApprovalRecord 未实际写入 | 审批历史无法查询 |
| 9 | 评审状态缺"草稿"和"审批中" | 流程不完整 |
| 10 | 用户管理无 UI（只有 API） | 系统管理无法使用 |
| 11 | WBS 为扁平列表（无层级树） | 多级任务分解无法表达 |
| 12 | 项目经理无法制定主计划里程碑 | PM 核心职责缺失 |
| 13 | 项目总调度/组员角色缺失 | 权限矩阵不完整 |
| 14 | 无部门管理 | 组织架构缺失 |

### P2 — 增强功能（提升体验）

| # | Gap |
|---|-----|
| 15 | 依赖类型只有 FS（缺 SS/FF/SF） |
| 16 | 任务状态（未开始/进行中/已完成/已延期）缺失 |
| 17 | 进度预警（绿/黄/橙/红）未实现 |
| 18 | 日志管理 UI 缺失 |
| 19 | 移动端响应式未支持 |
| 20 | 进度记录历史（pm_progress）未实现 |
| 21 | 计划调整历史版本管理缺失 |
| 22 | WBS 内嵌甘特图（PDF 图3布局）未实现 |
| 23 | 审批详情独立页面未实现 |
| 24 | 虚拟滚动未支持（任务多了会卡） |

### P3 — 优化项

| # | Gap |
|---|-----|
| 25 | searchFilter null 日期过滤 bug（BUG-P9-01/04/05） |
| 26 | Kanban drag-drop 状态持久化缺失 |
| 27 | 甘特图 SVG 渲染性能 |
| 28 | 多用户并发感知（WebSocket/SSE） |

---

## 十五、建议实施顺序

```
P0 第一波（安全合规，不能再拖）:
  1. JWT 登录 + 密码 bcrypt 加密
  2. SQL 注入/XSS 输入校验
  3. CORS 白名单

P1 第二波（核心功能补全）:
  4. Dashboard/首页（待办/进度/里程碑/快捷入口）
  5. 任务进度 progress_percent 字段 + 填报 UI
  6. 站内消息通知面板 + 未读计数
  7. ApprovalRecord 实际写入
  8. 评审状态补全（草稿/审批中）
  9. 用户管理 UI + 角色管理 UI
  10. 项目基本信息补全（编号/日期/状态）
  11. WBS 层级树（parent_task_id）
  12. 项目里程碑 + PM 主计划入口
  13. 部门管理 CRUD

P2 第三波（增强体验）:
  14. 依赖类型 SS/FF/SF
  15. 任务状态（未开始/进行中/已完成）
  16. 进度预警颜色
  17. 日志管理 UI
  18. 移动端适配
  19. WBS 内嵌甘特图
  20. 审批详情独立页
```
