# T2.5 — Dependency 端点（复用 T1.2/T1.3）

## 目标

实现任务依赖设置端点：调用 shared 层 `detectCycle`、`setDependency`、`propagateFinishChange`。

## 前置

- T1.2、T1.3 完成

## 交付物

- `packages/backend/src/tasks/tasks.controller.ts`（补充端点）
- `packages/backend/src/tasks/tasks.service.ts`（补充方法）
- `packages/backend/src/tasks/tasks.service.spec.ts`（补充测试）

## 命中的用例 ID

- FT-DEP-01..05

## 先写的测试（RED）

```typescript
describe('TasksService dependency', () => {
  it('FT-DEP-01: set valid dependency → 200, new edge in graph', async () => { })
  it('FT-DEP-02: creates cycle → 422 CYCLE + cycle path ids', async () => { })
  it('FT-DEP-03: task already has dep + set second → 422 ONE_TO_ONE_VIOLATION', async () => { })
  it('FT-DEP-04: upstream finish changes → returns affected downstream diff list', async () => { })
  it('FT-DEP-05: cross-group dep, downstream not approved → still recalc, pendingRecalc=true', async () => { })
})
```

## 验收清单

- [ ] `PUT /tasks/:id/dependency` 有效依赖设置成功
- [ ] 循环检测返回 422 + 回路 ids
- [ ] 1-to-1 违规返回 422
- [ ] 上游 finish 变更返回下游 diff 列表
- [ ] FT-DEP-01..05 通过

## 不做什么

- 前端依赖选择器 UI（放 T3.6）
