# T1.2 — 依赖图 1-to-1 约束 + DFS 循环检测

## 目标

实现 `packages/shared/src/dependency.ts`：`detectCycle`、`setDependency`、时间联动传播。

## 前置

- T1.1 完成

## 交付物

- `packages/shared/src/dependency.ts`
- `packages/shared/src/dependency.test.ts`

## 命中的用例 ID

- DT-DEP-01..06（6 条）
- DT-CAL-06..08（时间联动 3 条）

## 先写的测试（RED）

```typescript
// dependency.test.ts
describe('detectCycle', () => {
  it('DT-DEP-01: empty graph → no cycle', () => {
    expect(detectCycle(new Map())).toEqual({ hasCycle: false })
  })

  it('DT-DEP-02: self-loop A→A → CYCLE_SELF', () => {
    const graph = new Map([['A', new Set(['A'])]])
    expect(detectCycle(graph)).toMatchObject({ hasCycle: true, type: 'CYCLE_SELF', path: ['A', 'A'] })
  })

  it('DT-DEP-03: two-node A→B→A → CYCLE', () => {
    // ...
  })

  it('DT-DEP-04: chain A→B→C→A → full cycle path', () => {
    // ...
  })

  it('DT-DEP-05: DAG → no cycle', () => {
    // ...
  })
})

describe('setDependency', () => {
  it('DT-DEP-06: task with existing dep → ONE_TO_ONE_VIOLATION', () => {
    // ...
  })
})
```

## 实现要点

```typescript
export type CycleDetectionResult =
  | { hasCycle: false }
  | { hasCycle: true; type: 'CYCLE_SELF'; path: [string, string] }
  | { hasCycle: true; type: 'CYCLE'; path: string[] }

export function detectCycle(graph: Map<string, Set<string>>): CycleDetectionResult
export function setDependency(
  taskId: string,
  depId: string | null,
  existingTasks: Task[]
): { ok: true; updatedTasks: Task[] } | { ok: false; code: 'ONE_TO_ONE_VIOLATION' | 'CYCLE'; cyclePath?: string[] }
export function propagateFinishChange(
  changedTask: Task,
  allTasks: Task[],
  calendar: WorkCalendar
): TaskPatch[]
```

## 验收清单

- [ ] `pnpm --filter shared test` 全绿（DT-DEP-01..06 + DT-CAL-06..08）
- [ ] 行覆盖率 ≥ 95%

## 不做什么

- 不调用任何 I/O
- 不做 API 调用
