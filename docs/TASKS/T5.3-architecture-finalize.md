# T5.3 — `docs/ARCHITECTURE.md` 定稿 + UI 参考图嵌入

## 目标

将 `docs/ARCHITECTURE.md` 补充完整（补充运行时的数据流图、模块依赖图），并嵌入 `docx-extract` 产出的 UI 参考图。

## 前置

- T0.6（docx-extract 已运行）
- 所有 T1.x–T4.x 完成

## 交付物

- `docs/ARCHITECTURE.md`（定稿版）
- `docs/assets/ui-ref/` 内容（已由 T0.6 产出）

## 定稿内容

1. 补充 **模块依赖图**（mermaid）：`shared` 被哪些模块引用、backend 的 module 间依赖关系
2. 补充 **数据流图**：从 GL 编辑 → 自动保存草稿 → submit → PM 审批 → 主从同步 → Outbox 的完整数据流
3. 嵌入 UI 参考图：在 `docs/assets/ui-ref/` 目录下，在 `ARCHITECTURE.md` 中用 `![](../assets/ui-ref/img01.png)` 引用
4. 补充 **扩展点文档**：备忘录、钉钉、导出三处 stub 的替换方案（如接入真实钉钉需要的改动清单）

## 验收

- [ ] `docs/ARCHITECTURE.md` 中所有 mermaid 图可渲染
- [ ] UI 参考图正确引用
- [ ] 扩展点文档完整

## 不做什么

- 不修改已实现的业务代码
