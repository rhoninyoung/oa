# T3.5 — 审批面板（提交/撤回/同意/拒绝/重新排期 + 理由校验）

## 目标

实现 `ApprovalPanel` 组件：GL 提交/撤回，PM 同意/拒绝/重新排期，理由字段校验。

## 前置

- T3.2 完成（WBS 表格已可用）

## 交付物

- `packages/frontend/src/components/ApprovalPanel/ApprovalPanel.tsx`
- `packages/frontend/src/components/ApprovalPanel/ApprovalPanel.test.tsx`

## 命中的用例 ID

- E2E-SM-01（提交 → PM badge）、E2E-SM-03（拒绝理由校验）、E2E-SM-04（冲突 banner）
- FT-APPROVE-01..03（理由校验在 FT 层通过 API 测试覆盖）

## 先写的测试（RED）

```typescript
describe('ApprovalPanel', () => {
  describe('GL view', () => {
    it('shows "提交" button when PENDING', async () => { })
    it('shows "撤回" button when REVIEWING', async () => { })
    it('shows REJECTED badge + reason when REJECTED', async () => { })
  })

  describe('PM view', () => {
    it('shows "同意" / "拒绝" / "重新排期" buttons when REVIEWING', async () => { })
    it('"拒绝" button disabled when reason is empty', async () => { })
    it('"拒绝" button disabled when reason > 200 chars', async () => { })
    it('"重新排期" triggers OVERWRITE_RISK if GL is editing', async () => { })
  })
})
```

## 验收清单

- [ ] GL 看到提交/撤回按钮（按状态显示）
- [ ] PM 看到同意/拒绝/重新排期按钮
- [ ] 拒绝理由 textarea < 10 字时按钮 disabled
- [ ] 拒绝理由 textarea > 200 字时出现截断提示
- [ ] GL 侧 REJECTED 状态显示拒绝理由
- [ ] E2E-SM-01、E2E-SM-03 通过

## 不做什么

- 不实现状态机逻辑（后端已有）
