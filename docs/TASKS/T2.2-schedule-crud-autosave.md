# T2.2 — GroupSchedule CRUD + 草稿自动保存（乐观锁 `version`）

## 目标

实现 `SchedulesModule`：Schedule CRUD、草稿保存、乐观锁版本检测。

## 前置

- T1.1、T2.1 完成

## 交付物

- `packages/backend/src/schedules/schedules.controller.ts`
- `packages/backend/src/schedules/schedules.service.ts`
- `packages/backend/src/schedules/schedules.service.spec.ts`

## 命中的用例 ID

- FT-SAVE-01、FT-SAVE-02
- DT-SAVE-01..03（hook 层，在 T3.4）
- DT-LOCK-01

## 先写的测试（RED）

```typescript
describe('SchedulesService', () => {
  describe('FT-SAVE-01', () => {
    it('draft patch with matching version → 200, version incremented', async () => {
      // PATCH /schedules/:id/draft
    })
  })

  describe('FT-SAVE-02', () => {
    it('concurrent draft patches → second gets 409 with latestVersion', async () => {
      // Test optimistic locking
    })
  })

  describe('DT-LOCK-01', () => {
    it('applyPatch with stale version → VERSION_CONFLICT', async () => {
      // Unit test for applyPatch pure function
    })
  })
})
```

## 验收清单

- [ ] `GET /schedules/:id` 返回 Schedule + Tasks
- [ ] `PATCH /schedules/:id/draft` 乐观锁正常
- [ ] `version` 不匹配时返回 409，含 `latestVersion`
- [ ] FT-SAVE-01、FT-SAVE-02、DT-LOCK-01 通过

## 不做什么

- 不实现状态转换（submit/approve 等，放 T2.4）
