# T1.3 — 工作日历 + 时间联动

## 目标

实现 `packages/shared/src/calendar.ts`：`addWorkDays`、`isWorkDay` 及时间联动 `propagateSchedule`。

## 前置

- T1.1 完成（依赖状态机上下文类型）

## 交付物

- `packages/shared/src/calendar.ts`
- `packages/shared/src/calendar.test.ts`

## 命中的用例 ID

- DT-CAL-01..08（8 条，其中 DT-CAL-06..08 与 T1.2 共用）

## 先写的测试（RED）

```typescript
// calendar.test.ts
describe('addWorkDays', () => {
  it('DT-CAL-01: Monday + 1 workDay = Tuesday', () => {
    const cal = new WorkCalendar([])
    expect(cal.addWorkDays('2026-04-20', 1)).toEqual('2026-04-21')
  })

  it('DT-CAL-02: Friday + 1 workDay = next Monday', () => {
    const cal = new WorkCalendar([])
    expect(cal.addWorkDays('2026-04-24', 1)).toEqual('2026-04-27')
  })

  it('DT-CAL-03: Friday + 3 workDays = next Wednesday', () => {
    const cal = new WorkCalendar([])
    expect(cal.addWorkDays('2026-04-24', 3)).toEqual('2026-04-29')
  })

  it('DT-CAL-04: Friday as holiday + 2 workDays = Tuesday', () => {
    const cal = new WorkCalendar(['2026-04-24']) // 假设2026-04-24是周五且为假日
    expect(cal.addWorkDays('2026-04-23', 2)).toEqual('2026-04-28')
  })

  it('DT-CAL-05: duration=0 → same day', () => {
    const cal = new WorkCalendar([])
    expect(cal.addWorkDays('2026-04-20', 0)).toEqual('2026-04-20')
  })
})

describe('propagateFinishChange', () => {
  it('DT-CAL-06: downstream start = dep.finish + 1 workDay', () => {
    // ...
  })

  it('DT-CAL-07: chain propagation B→C,D', () => {
    // ...
  })

  it('DT-CAL-08: upstream finish moves earlier → downstream shifts', () => {
    // ...
  })
})
```

## 实现要点

```typescript
export class WorkCalendar {
  constructor(private holidays: string[] = []) {}

  isWorkDay(date: string): boolean {
    const d = new Date(date)
    const dayOfWeek = d.getDay()
    if (dayOfWeek === 0 || dayOfWeek === 6) return false
    if (this.holidays.includes(date)) return false
    return true
  }

  addWorkDays(startDate: string, days: number): string {
    // 从 startDate（包含）之后数 days 个工作日，返回结束日期
  }
}
```

工作日历默认内置周末屏蔽，节假日通过构造函数注入（可配置 holiday list）。

## 验收清单

- [ ] `pnpm --filter shared test` 全绿（DT-CAL-01..05，DT-CAL-06..08 已在 T1.2 覆盖）
- [ ] 行覆盖率 ≥ 95%

## 不做什么

- 不做中国法定节假日 API 调用（内置节假日表为可配置 stub）
