# T3.6 — 依赖选择器（点击单元格 → 其他列置灰 → 目标行高亮 → Esc 退出）

## 目标

实现 `DependencyPicker` 组件：点击依赖列单元格后进入选择模式，其他列禁用，鼠标悬停行高亮，选择后保存。

## 前置

- T3.2 完成

## 交付物

- `packages/frontend/src/components/DependencyPicker/DependencyPicker.tsx`
- `packages/frontend/src/components/DependencyPicker/DependencyPicker.test.tsx`

## 命中的用例 ID

- FT-TBL-04
- E2E-DEP-01、E2E-DEP-02、E2E-DEP-04

## 先写的测试（RED）

```typescript
describe('DependencyPicker', () => {
  it('FT-TBL-04: click dependency cell → other columns aria-disabled=true', async () => {
    // ...
  })

  it('FT-TBL-04: target rows highlighted on hover', async () => { })

  it('FT-TBL-04: Esc cancels and restores normal state', async () => { })

  it('E2E-DEP-01: select T1 for T2 → arrow drawn from T1 to T2', async () => { })

  it('E2E-DEP-02: selecting T1 as dep of T2 when T2→T1 exists → modal with cycle message', async () => { })

  it('E2E-DEP-04: selected row shows dep indicator', async () => { })
})
```

## 验收清单

- [ ] 点击依赖单元格 → 进入选择模式
- [ ] 非依赖列 `aria-disabled=true`
- [ ] 鼠标悬停目标行高亮
- [ ] 点击目标行 → 设置依赖 → 退出选择模式
- [ ] Esc → 取消，退出选择模式
- [ ] 循环依赖时弹窗提示（文案来自后端）
- [ ] FT-TBL-04、E2E-DEP-01、E2E-DEP-02、E2E-DEP-04 通过

## 不做什么

- 不实现时间联动计算（后端已有）
