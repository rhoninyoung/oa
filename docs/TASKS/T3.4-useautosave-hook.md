# T3.4 — `useAutoSave` Hook（30s 节流 + 失败重试 + 冲突提示）

## 目标

实现 `useAutoSave` React Hook：依赖变化后 30s 触发保存，失败指数退避重试，组件卸载清理。

## 前置

- T2.2 完成（后端草稿保存端点可用）

## 交付物

- `packages/frontend/src/hooks/useAutoSave.ts`
- `packages/frontend/src/hooks/useAutoSave.test.ts`

## 命中的用例 ID

- DT-SAVE-01..03
- FT-SAVE-01、FT-SAVE-02
- E2E-SAVE-01

## 先写的测试（RED）

```typescript
// useAutoSave.test.ts
describe('useAutoSave', () => {
  it('DT-SAVE-01: does not call saveFn before 30s', async () => {
    const saveFn = vi.fn()
    render(<Comp deps={['a']} saveFn={saveFn} />)
    fireEvent.change(screen.getByRole('textbox'), { target: { value: 'x' } })
    await vi.advanceTimersByTime(29_000)
    expect(saveFn).not.toHaveBeenCalled()
  })

  it('DT-SAVE-01: calls saveFn after 30s', async () => {
    // ...
  })

  it('DT-SAVE-02: component unmount cancels pending timer', async () => {
    const { unmount } = render(<Comp deps={['a']} saveFn={saveFn} />)
    unmount()
    await vi.advanceTimersByTime(30_000)
    expect(saveFn).not.toHaveBeenCalled()
  })

  it('DT-SAVE-03: exponential backoff on failure, throws after 3 retries', async () => {
    // ...
  })
})
```

## 验收清单

- [ ] 编辑后 30s 触发 `PATCH /schedules/:id/draft`
- [ ] 编辑后 29s 再次编辑 → 重新计时 30s
- [ ] 组件卸载清除定时器（无内存泄漏）
- [ ] 失败后重试 3 次（指数退避），再失败抛给 UI 显示错误条
- [ ] DT-SAVE-01..03、FT-SAVE-01、E2E-SAVE-01 通过

## 不做什么

- 不实现冲突弹窗 UI（由使用方组件处理）
