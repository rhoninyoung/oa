# T2.7 — `NotificationOutbox` + 钉钉 stub worker

## 目标

实现 `OutboxModule`：事件持久化 + 定时 worker 消费（仅打印结构化 JSON 到控制台）。

## 前置

- T2.4 完成（状态转换已产生 Outbox 记录）

## 交付物

- `packages/backend/src/outbox/outbox.module.ts`
- `packages/backend/src/outbox/outbox.service.ts`
- `packages/backend/src/outbox/outbox.worker.ts`
- `packages/backend/src/outbox/outbox.service.spec.ts`

## 命中的用例 ID

- DT-OBX-01
- FT-OBX-01、FT-OBX-02
- E2E-NOTIF-01（部分）

## 先写的测试（RED）

```typescript
describe('OutboxService', () => {
  it('DT-OBX-01: buildNotification idempotency key is stable', () => {
    // Same type|scheduleId|action|version → same key
  })

  it('FT-OBX-01: worker tick → dispatchedAt set, console has structured JSON', async () => {
    // Mock console.log, call worker, assert JSON in output
  })

  it('FT-OBX-02: duplicate idempotency keys → only latest kept', async () => {
    // 入列两条同一 key 的 notification，只保留最新未派发的
  })
})
```

## 验收清单

- [ ] `SCHEDULE_SUBMITTED`、`SCHEDULE_APPROVED`、`RESCHEDULE` 事件写入 `NotificationOutbox`
- [ ] Worker 定时调度，`dispatchedAt` 更新
- [ ] 控制台输出结构化 JSON：`{ type, payload, at }`
- [ ] 幂等键重复时只保留最新

## 不做什么

- 不接入真实钉钉 API
