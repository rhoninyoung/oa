# T3.2 — WBS 表格骨架（`@tanstack/react-table` + 自建键盘层）

## 目标

基于 `@tanstack/react-table` 构建 WBS 表格基础骨架：列定义、行渲染、虚拟滚动占位。

## 前置

- T0.4 完成（T3.1 可并行）

## 交付物

- `packages/frontend/src/components/WbsTable/WbsTable.tsx`
- `packages/frontend/src/components/WbsTable/useWbsTable.ts`（封装 `@tanstack/react-table`）
- `packages/frontend/src/components/WbsTable/WbsTable.test.tsx`
- `packages/frontend/src/components/WbsTable/columns.ts`（列定义）

## 命中的用例 ID

- DT-TBL-01（`normalizeRange`，在 table hook 中）
- DT-TBL-08（冻结列 metadata）
- FT-TBL-04（列禁用状态）
- FT-TBL-05（冻结+右键菜单）

## 先写的测试（RED）

```typescript
describe('WbsTable', () => {
  it('renders tasks as rows', async () => { })
  it('first column is sticky on horizontal scroll', async () => { /* DT-TBL-08 */ })
  it('dependency column click → other columns aria-disabled', async () => { /* FT-TBL-04 */ })
  it('right-click → context menu with insert/delete row', async () => { /* FT-TBL-05 */ })
})
```

## 验收清单

- [ ] 列：序号、任务名、负责人、开始日期、结束日期、持续天数、依赖
- [ ] 行冻结首列（序号）
- [ ] 右键菜单「插入行」/「删除行」存在
- [ ] 基础渲染测试通过

## 不做什么

- 不实现复制/粘贴/Undo/Redo/Alt+Enter（放 T3.3）
