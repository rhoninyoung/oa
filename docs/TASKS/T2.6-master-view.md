# T2.6 — Master 视图 + PM 新增/删除 `MASTER` 行（删除约束）

## 目标

实现 `MasterModule`：总表视图、PM 新增/删除 `source=MASTER` 行、删除约束强制执行。

## 前置

- T2.3、T2.4 完成

## 交付物

- `packages/backend/src/master/master.controller.ts`
- `packages/backend/src/master/master.service.ts`
- `packages/backend/src/master/master.service.spec.ts`

## 命中的用例 ID

- FT-MASTER-01..06
- FT-MASTER-04（删除约束 P0）
- FT-MASTER-02（反向同步）
- FT-MASTER-06（PM vs GL 权限）

## 先写的测试（RED）

```typescript
describe('MasterService', () => {
  it('FT-MASTER-01: approved schedule → tasks appear in master view', async () => { })
  it('FT-MASTER-02: PM adds row → appended to group as source=MASTER, Outbox x2', async () => { })
  it('FT-MASTER-03: GL sees MASTER rows as read-only with field list', async () => { })
  it('FT-MASTER-04: delete source=MASTER → 200; delete source=GROUP → 422 SYNC_ROW_READONLY', async () => {
    // P0
  })
  it('FT-MASTER-05: reschedule → group tasks removed from master', async () => { })
  it('FT-MASTER-06: PM change MASTER row owner → allowed; GL → 403', async () => { })
})
```

## 验收清单

- [ ] `GET /master/:iterationId` 返回 APPROVED 组任务 + source=MASTER 行
- [ ] `POST /master/:iterationId/rows`（PM）→ source=MASTER + Outbox x2
- [ ] `DELETE /master/rows/:taskId` source=MASTER 可删，source=GROUP 返回 422
- [ ] FT-MASTER-01..06 通过

## 不做什么

- 前端总表页面（放 T3.7）
