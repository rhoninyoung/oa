# T4.5 — E2E: 依赖循环检测 + 时间联动

## 目标

Playwright E2E：依赖设置 → 循环检测拦截 → 时间联动（跨周末验证）。

## 前置

- T3.6 完成

## 命中的用例 ID

- E2E-DEP-01（[@p0]）
- E2E-DEP-02（[@p0]）
- E2E-DEP-03
- E2E-DEP-04

## 场景步骤

### Part A — 循环检测

1. GL 进入 G1 排期表，任务 T1、T2 存在
2. 点击 T2 的依赖列，选择 T1（设置 T2→T1 依赖）→ 保存成功，箭头可见
3. 再次点击 T1 的依赖列，选择 T2（T1→T2）
4. 断言弹窗出现，文案包含 "存在循环依赖：T1→T2→T1"
5. 保存被拦截，T1 依赖不变

### Part B — 时间联动（跨周末）

6. T1 finish 从周五（2026-04-24）改为周二（2026-04-21）
7. 等待 API 返回
8. 断言 T2 start 变为下周一（2026-04-27，跨周末）
9. 断言变更的单元格有高亮

### Part C — 依赖选择器交互

10. 点击 T3 依赖列 → 其他列置灰
11. 鼠标悬停 T4 行 → 高亮
12. 点击 T4 → T3 显示依赖 T4 的标识
13. 按 Esc → 退出选择模式

## 验收

- [ ] E2E-DEP-01、E2E-DEP-02、E2E-DEP-03、E2E-DEP-04 通过

## 不做什么

- 不重复测总表功能
