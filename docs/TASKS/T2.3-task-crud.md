# T2.3 — Task CRUD（增删行、排序、字段校验）

## 目标

实现 `TasksModule`：Task 的增删改、orderIndex 排序、Zod 字段校验。

## 前置

- T2.2 完成

## 交付物

- `packages/backend/src/tasks/tasks.controller.ts`
- `packages/backend/src/tasks/tasks.service.ts`
- `packages/backend/src/tasks/tasks.service.spec.ts`

## 命中的用例 ID

- DT-TBL-06（`insertRowBelow` 在 service 层实现）
- DT-TBL-07（删除权限判断，可抽为纯函数）

## 先写的测试（RED）

```typescript
describe('TasksService', () => {
  it('insertRowBelow → orderIndex renumbered', async () => { /* DT-TBL-06 */ })
  it('delete task, source=MASTER → allowed', async () => { /* DT-TBL-07 */ })
  it('delete task, source=GROUP, non-PENDING → REJECTED', async () => {
    // FT-MASTER-04 依赖
  })
  it('PATCH task fields → Zod validation errors returned as 422', async () => {
    // invalid dates, negative duration
  })
})
```

## 验收清单

- [ ] `POST /schedules/:id/tasks` 插入行并重排 orderIndex
- [ ] `DELETE /tasks/:id` source=MASTER 可删，source=GROUP 非 PENDING 不可删
- [ ] `PATCH /tasks/:id` 字段校验返回 422
- [ ] orderIndex 在操作后连续稳定

## 不做什么

- 不实现依赖设置（放 T2.5）
