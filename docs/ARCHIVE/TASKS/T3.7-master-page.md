# T3.7 — 总表页面（PM 新增/删除行，行来源徽标）

## 目标

实现 PM 总表页面：汇总视图、source=MASTER 行新增/删除、行来源徽标区分。

## 前置

- T3.2、T3.5 完成

## 交付物

- `packages/frontend/src/pages/MasterPage/MasterPage.tsx`
- `packages/frontend/src/pages/MasterPage/MasterPage.test.tsx`

## 命中的用例 ID

- FT-MASTER-01..03、FT-MASTER-06（部分 UI）
- E2E-MASTER-01、E2E-MASTER-02

## 先写的测试（RED）

```typescript
describe('MasterPage', () => {
  it('shows all APPROVED group tasks + MASTER rows', async () => { })

  it('MASTER rows have distinct badge', async () => { })

  it('E2E-MASTER-01: PM adds row → row appears in GL view after navigation', async () => { })

  it('E2E-MASTER-02: delete MASTER row → success; delete GROUP row → toast error', async () => { })
})
```

## 验收清单

- [ ] 总表显示所有 APPROVED 组的任务（source=GROUP 徽标）
- [ ] 总表显示 PM 新增行（source=MASTER 徽标）
- [ ] 删除 source=GROUP 行 → toast 提示"系统同步行不可删除"
- [ ] 删除 source=MASTER 行 → 成功
- [ ] E2E-MASTER-01、E2E-MASTER-02 通过

## 不做什么

- 不实现审批操作（由 T3.5 ApprovalPanel 提供）
