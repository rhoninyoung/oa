# T3.1 — 角色切换器 + 项目/迭代侧边树

## 目标

实现前端 layout：顶部角色切换器 + 侧边项目/迭代树。

## 前置

- T0.4 完成

## 交付物

- `packages/frontend/src/components/Layout/Layout.tsx`
- `packages/frontend/src/components/RoleSwitcher/RoleSwitcher.tsx`
- `packages/frontend/src/components/Sidebar/ProjectTree.tsx`
- `packages/frontend/src/components/Sidebar/ProjectTree.test.tsx`

## 命中的用例 ID

- E2E-NAV-01（完整路由验证）
- E2E-PERM-01、E2E-PERM-02（跨组权限在路由层体现）

## 先写的测试（RED）

```typescript
// ProjectTree.test.tsx
it('shows project name after load', async () => {
  render(<ProjectTree />)
  await screen.findByText('OA 平台')
})
```

## 验收清单

- [ ] 角色切换器显示当前用户姓名+角色，可点击切换
- [ ] 切换角色后侧边树内容按权限变化（GL 只看本组，PM 看所有）
- [ ] 双击迭代 → 跳转 `/iterations/:id`
- [ ] E2E-NAV-01 通过

## 不做什么

- 不实现 WBS 表格
