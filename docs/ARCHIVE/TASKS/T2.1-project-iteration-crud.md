# T2.1 — Project / Iteration CRUD

## 目标

实现 `ProjectsModule` 和 `IterationsModule`：提供项目树查询接口。

## 前置

- T0.3 完成（backend 骨架 + Prisma）

## 交付物

- `packages/backend/src/projects/projects.controller.ts`
- `packages/backend/src/projects/projects.service.ts`
- `packages/backend/src/projects/projects.service.spec.ts`
- `packages/backend/src/iterations/iterations.controller.ts`
- `packages/backend/src/iterations/iterations.service.ts`
- `packages/backend/src/iterations/iterations.service.spec.ts`

## 命中的用例 ID

- FT-NAV-01、FT-NAV-02

## 先写的测试（RED）

```typescript
// projects.service.spec.ts
describe('ProjectsService', () => {
  describe('FT-NAV-01', () => {
    it('returns project+iteration tree filtered by role', async () => {
      // GL sees only own groups; PM sees all
    })
  })
})

// iterations.service.spec.ts
describe('IterationsService', () => {
  describe('FT-NAV-02', () => {
    it('returns all group schedule statuses for an iteration', async () => {
      // status summary for double-click entry
    })
  })
})
```

## 验收清单

- [ ] `GET /projects` 按角色过滤
- [ ] `GET /iterations/:id` 返回各组排期状态摘要
- [ ] FT-NAV-01、FT-NAV-02 通过

## 不做什么

- 不实现审批状态变更
- 不实现任务编辑
