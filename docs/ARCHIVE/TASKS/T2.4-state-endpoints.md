# T2.4 — 状态机端点：submit / withdraw / approve / reject / reschedule

## 目标

实现 `SchedulesModule` 中的状态转换端点，调用 shared 层 `canTransition` 并写 `ApprovalRecord`。

## 前置

- T1.1（状态机）、T2.2（Schedule CRUD）、T2.3（Task CRUD）完成

## 交付物

- `packages/backend/src/schedules/schedules.controller.ts`（补充端点）
- `packages/backend/src/schedules/schedules.service.ts`（补充方法）
- `packages/backend/src/schedules/schedules.service.spec.ts`（补充测试）

## 命中的用例 ID

- FT-SM-01..06（状态机 API）
- FT-APPROVE-01..03（审批）
- FT-RESCHED-01（重新排期）
- FT-SM-03、FT-SM-04（撤回）
- FT-LOCK-01（reschedule 覆盖风险）
- DT-SM-01..12（在 shared 层已覆盖，service 层测集成）

## 先写的测试（RED）

```typescript
describe('SchedulesService state transitions', () => {
  it('FT-SM-01: submit(PENDING, non-empty) → REVIEWING, version+1, Outbox entry', async () => { })
  it('FT-SM-02: submit with empty tasks → 422 CONTENT_EMPTY', async () => { })
  it('FT-SM-03: withdraw(REVIEWING) → PENDING, dismiss Outbox', async () => { })
  it('FT-SM-04: withdraw(non-REVIEWING) → 409 INVALID_STATE', async () => { })
  it('FT-SM-05: approve by PM → APPROVED, ApprovalRecord', async () => { })
  it('FT-SM-06: reject with invalid reason → 422', async () => { })
  it('FT-APPROVE-01: non-PM approve → 403', async () => { })
  it('FT-RESCHED-01: reschedule → REJECTED, Outbox, snapshot', async () => { })
  it('FT-LOCK-01: reschedule with stale draft version → OVERWRITE_RISK', async () => { })
})
```

## 验收清单

- [ ] `POST /schedules/:id/submit`（GL）→ REVIEWING
- [ ] `POST /schedules/:id/withdraw`（GL）→ PENDING
- [ ] `POST /schedules/:id/approve`（PM）→ APPROVED + ApprovalRecord
- [ ] `POST /schedules/:id/reject`（PM，reason 1-200字）→ REJECTED
- [ ] `POST /schedules/:id/reschedule`（PM）→ REJECTED + Outbox + snapshot
- [ ] 所有 FT-SM-*/FT-APPROVE-*/FT-RESCHED-*/FT-LOCK-01 通过

## 不做什么

- 不实现前端审批 UI（放 T3.5）
