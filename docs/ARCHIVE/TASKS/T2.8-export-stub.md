# T2.8 — Export stub（返回固定 xlsx buffer + 权限校验）

## 目标

实现 `ExportsModule`：导出端点 stub，返回正确 content-type + 非空 body，权限校验走 `permissions`。

## 前置

- T1.4、T2.6 完成

## 交付物

- `packages/backend/src/exports/exports.controller.ts`
- `packages/backend/src/exports/exports.service.ts`
- `packages/backend/src/exports/exports.service.spec.ts`

## 命中的用例 ID

- FT-EXP-01、FT-EXP-02

## 先写的测试（RED）

```typescript
describe('ExportsService', () => {
  it('FT-EXP-01: GET /exports/iterations/:id.xlsx → content-type application/vnd.openxmlformats..., non-empty body', async () => { })

  it('FT-EXP-02: GL requests export of other group → 403', async () => { })
})
```

## 验收清单

- [ ] `GET /exports/iterations/:id.xlsx` 返回正确 content-type
- [ ] GL 导出他组总表返回 403
- [ ] body 非空（MVP 返回固定 buffer 即可）

## 不做什么

- 不生成真实 Excel 内容（留后续扩展）
