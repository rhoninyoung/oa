# T5.1 — 种子数据 + README 运行指引

## 目标

编写完整的种子数据和 README，让新开发者克隆后一键启动完整环境。

## 前置

- 所有 T2.x、T3.x 任务完成

## 交付物

- `packages/backend/prisma/seed.ts`（完整种子数据）
- `README.md`（项目运行指南）

## 种子数据

```
1 Project
  └─ 1 Iteration ("2026-Q2 迭代", 2026-04-01 ~ 2026-06-30)

2 Groups
  ├─ G1 ("前端组")
  │   └─ GL: U1 ("胡孟瑶")
  └─ G2 ("后端组")
      └─ GL: U2 ("陈思远")

1 PM: P1 ("王架构")
```

初始状态：U1 的 G1 Schedule 处于 PENDING，含 3 个空任务行。

## README 内容

- 环境要求（Node 20+、pnpm、Docker）
- 一键启动：`docker-compose up -d && pnpm install && pnpm --filter backend prisma migrate dev && pnpm --filter backend prisma db seed && pnpm dev`
- 测试命令：`pnpm test`（lint+DT）、`pnpm --filter backend test`（FT）、`pnpm --filter e2e test`（E2E）
- 角色切换说明（localStorage / header）
- 常见问题

## 验收

- [ ] 新克隆仓库可按 README 跑通所有服务
- [ ] Seed 数据使所有 E2E 场景可独立运行

## 不做什么

- 不写 API 文档（用代码生成或 Swagger）
