# T5.2 — 覆盖率阈值 + CI 门禁定稿

## 目标

配置 CI 覆盖率门禁，更新 `docs/TEST-PLAN.md` 最终状态，更新 `CI.yml` 质量阈值。

## 前置

- 所有 T1.x、T2.x、T3.x 完成，覆盖率数据可用

## 交付物

- `.github/workflows/ci.yml`（更新覆盖率 gate）
- `docs/TEST-PLAN.md`（更新为最新覆盖状态）

## 覆盖率门禁配置

```yaml
- name: Check coverage
  run: |
    pnpm --filter shared test -- --coverage --coverageReporters=lcov
    pnpm --filter backend test -- --coverage --coverageReporters=lcov
    pnpm --filter frontend test -- --coverage --coverageReporters=lcov
  # thresholds enforced by jest/vitest config:
  # shared: 95%, backend service: 85%, frontend: 75%
```

## 验收

- [ ] CI 覆盖率 gate 配置完成
- [ ] `docs/TEST-PLAN.md` 所有条目标记为 `[x]`（或保留 open issue 链接）
- [ ] `jest.config.ts` / `vitest.config.ts` 中 threshold 配置与需求一致

## 不做什么

- 不调整测试用例本身（只配置阈值）
