{
  "name": "oa-mvp-html",
  "version": "1.0.0",
  "description": "OA Project Management MVP — single-file HTML, localStorage, no build",
  "type": "module",
  "scripts": {
    "test": "node --test",
    "serve": "python3 -m http.server 8080",
    "smoke": "python3 -m http.server 8080 & sleep 2 && echo 'Open http://localhost:8080'"
  },
  "engines": {
    "node": ">=18"
  }
}
